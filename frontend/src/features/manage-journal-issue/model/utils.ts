/**
 * @deprecated Import from `@/entities/journal-issue` instead.
 */
export { formatDate, formatDateTime, formatFileSize, fileNameFromStoragePath, issueStatusLabels, accessTypeLabels, publicAccessBadgeLabels, getJournalIssuePath, generateIssueId, validateCoverFile, validatePdfFile, COVER_ACCEPT, PDF_ACCEPT, COVER_MAX_BYTES, PDF_MAX_BYTES } from "@/entities/journal-issue";
